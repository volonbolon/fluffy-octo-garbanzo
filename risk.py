import pmf
from pregnancies import  Pregnancies

def calculate_arrival_probabilities():
    pass
    # pregs = Pregnancies()
    # pregs.read_records()
    # firsts, others = pregs.partition_between_first_and_others()
    #
    # first_records = [p.prglength for p in firsts.records]
    # first_pmf = pmf.make_pmf_from_list(first_records, name="First")
    #
    # others_records = [p.prglength for p in others.records]
    # others_hist = pmf.make_pmf_from_list(others_records, name="Others")
    #
    # for v in first_pmf.values

